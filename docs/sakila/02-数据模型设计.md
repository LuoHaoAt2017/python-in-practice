# Sakila数据库项目 - 数据模型设计

## 概述

Sakila数据库采用标准的关系型数据库设计，针对DVD租赁业务场景进行优化。数据库模型设计遵循规范化原则，同时考虑业务查询性能需求。

## 核心实体关系

### 1. 核心业务实体

```
Film (电影)
    ├── Actor (演员) - 多对多关系 (FilmActor)
    ├── Category (分类) - 多对多关系 (FilmCategory)
    ├── Language (语言) - 外键关联
    ├── Inventory (库存) - 一对多关系
    └── Rental (租赁) - 通过库存间接关联

Customer (客户)
    ├── Address (地址) - 外键关联
    ├── Store (商店) - 外键关联
    ├── Rental (租赁) - 一对多关系
    └── Payment (支付) - 一对多关系

Rental (租赁)
    ├── Inventory (库存) - 外键关联
    ├── Customer (客户) - 外键关联
    ├── Staff (员工) - 外键关联
    └── Payment (支付) - 一对一关系
```

## 详细模型设计

### 1. Actor (演员模型)

#### 表结构
```sql
CREATE TABLE actor (
    actor_id SERIAL PRIMARY KEY,
    first_name VARCHAR(45) NOT NULL,
    last_name VARCHAR(45) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

#### 模型定义
```python
class Actor(Base, TimestampMixin):
    __tablename__ = "actor"

    actor_id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    first_name: Mapped[str] = mapped_column(String(45), nullable=False)
    last_name: Mapped[str] = mapped_column(String(45), nullable=False)

    # 关系：演员可以参演多部电影
    film_actors: Mapped[List["FilmActor"]] = relationship(
        "FilmActor", back_populates="actor", cascade="all, delete-orphan"
    )
```

#### 设计特点
- 简单的演员信息存储
- 通过FilmActor实现多对多关系
- 自动时间戳管理

### 2. Film (电影模型)

#### 表结构
```sql
CREATE TABLE film (
    film_id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT,
    release_year INTEGER,
    language_id INTEGER NOT NULL,
    original_language_id INTEGER,
    rental_duration INTEGER NOT NULL DEFAULT 3,
    rental_rate NUMERIC(4,2) NOT NULL DEFAULT 4.99,
    length INTEGER,
    replacement_cost NUMERIC(5,2) NOT NULL DEFAULT 19.99,
    rating VARCHAR(5) DEFAULT 'G',
    special_features VARCHAR(100),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    FOREIGN KEY (language_id) REFERENCES language(language_id),
    FOREIGN KEY (original_language_id) REFERENCES language(language_id)
);
```

#### 模型定义
```python
class Film(Base, TimestampMixin):
    __tablename__ = "film"

    film_id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    title: Mapped[str] = mapped_column(String(255), nullable=False, index=True)
    description: Mapped[Optional[str]] = mapped_column(Text)
    release_year: Mapped[Optional[int]] = mapped_column(Integer)
    language_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("language.language_id"), nullable=False
    )
    original_language_id: Mapped[Optional[int]] = mapped_column(
        Integer, ForeignKey("language.language_id")
    )
    rental_duration: Mapped[int] = mapped_column(Integer, nullable=False, default=3)
    rental_rate: Mapped[float] = mapped_column(Numeric(4, 2), nullable=False, default=4.99)
    length: Mapped[Optional[int]] = mapped_column(Integer)
    replacement_cost: Mapped[float] = mapped_column(
        Numeric(5, 2), nullable=False, default=19.99
    )
    rating: Mapped[Optional[str]] = mapped_column(String(5), default="G")
    special_features: Mapped[Optional[str]] = mapped_column(String(100))

    # 关系：多对多关联表
    film_actors: Mapped[List["FilmActor"]] = relationship(
        "FilmActor", back_populates="film", cascade="all, delete-orphan"
    )
    film_categories: Mapped[List["FilmCategory"]] = relationship(
        "FilmCategory", back_populates="film", cascade="all, delete-orphan"
    )
    inventories: Mapped[List["Inventory"]] = relationship(
        "Inventory", back_populates="film", cascade="all, delete-orphan"
    )
```

#### 设计特点
- 完整的电影信息存储
- 支持多语言（原始语言和发行语言）
- 租赁业务相关字段（租期、租金、赔偿成本）
- 多对多关系通过关联表实现
- 索引优化（标题索引）

### 3. Customer (客户模型)

#### 表结构
```sql
CREATE TABLE customer (
    customer_id SERIAL PRIMARY KEY,
    store_id INTEGER NOT NULL,
    first_name VARCHAR(45) NOT NULL,
    last_name VARCHAR(45) NOT NULL,
    email VARCHAR(50),
    address_id INTEGER NOT NULL,
    activebool BOOLEAN NOT NULL DEFAULT TRUE,
    create_date TIMESTAMP NOT NULL,
    last_update TIMESTAMP,
    active INTEGER,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    FOREIGN KEY (store_id) REFERENCES store(store_id),
    FOREIGN KEY (address_id) REFERENCES address(address_id)
);
```

#### 模型定义
```python
class Customer(Base, TimestampMixin):
    __tablename__ = "customer"

    customer_id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    store_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("store.store_id"), nullable=False
    )
    first_name: Mapped[str] = mapped_column(String(45), nullable=False)
    last_name: Mapped[str] = mapped_column(String(45), nullable=False)
    email: Mapped[Optional[str]] = mapped_column(String(50))
    address_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("address.address_id"), nullable=False
    )
    activebool: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True)
    create_date: Mapped[datetime] = mapped_column(DateTime, nullable=False)
    last_update: Mapped[Optional[datetime]] = mapped_column(DateTime)
    active: Mapped[Optional[int]] = mapped_column(Integer)

    # 关系：与商店、地址、租赁、支付关联
    store: Mapped["Store"] = relationship("Store", back_populates="customers")
    address: Mapped["Address"] = relationship("Address", back_populates="customers")
    payments: Mapped[List["Payment"]] = relationship(
        "Payment", back_populates="customer", cascade="all, delete-orphan"
    )
    rentals: Mapped[List["Rental"]] = relationship(
        "Rental", back_populates="customer", cascade="all, delete-orphan"
    )
```

#### 设计特点
- 客户基本信息管理
- 双重激活状态（activebool和active）
- 与商店、地址的关联
- 租赁和支付历史记录

### 4. Rental (租赁模型)

#### 表结构
```sql
CREATE TABLE rental (
    rental_id SERIAL PRIMARY KEY,
    rental_date TIMESTAMP NOT NULL,
    inventory_id INTEGER NOT NULL,
    customer_id INTEGER NOT NULL,
    return_date TIMESTAMP,
    staff_id INTEGER NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    FOREIGN KEY (inventory_id) REFERENCES inventory(inventory_id),
    FOREIGN KEY (customer_id) REFERENCES customer(customer_id),
    FOREIGN KEY (staff_id) REFERENCES staff(staff_id)
);
```

#### 模型定义
```python
class Rental(Base, TimestampMixin):
    __tablename__ = "rental"

    rental_id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    rental_date: Mapped[datetime] = mapped_column(DateTime, nullable=False)
    inventory_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("inventory.inventory_id"), nullable=False
    )
    customer_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("customer.customer_id"), nullable=False
    )
    return_date: Mapped[Optional[datetime]] = mapped_column(DateTime)
    staff_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("staff.staff_id"), nullable=False
    )

    # 关系：与库存、客户、员工、支付关联
    inventory: Mapped["Inventory"] = relationship("Inventory", back_populates="rentals")
    customer: Mapped["Customer"] = relationship("Customer", back_populates="rentals")
    staff: Mapped["Staff"] = relationship("Staff", back_populates="rentals")
    payment: Mapped[Optional["Payment"]] = relationship(
        "Payment", back_populates="rental", uselist=False
    )
```

#### 设计特点
- 核心业务实体：租赁记录
- 租赁日期和归还日期管理
- 多方关联：库存、客户、员工
- 一对一支付关联

### 5. 多对多关联表

#### FilmActor (电影-演员关联表)
```python
class FilmActor(Base):
    __tablename__ = "film_actor"

    actor_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("actor.actor_id"), primary_key=True
    )
    film_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("film.film_id"), primary_key=True
    )

    # 双向关系
    actor: Mapped["Actor"] = relationship("Actor", back_populates="film_actors")
    film: Mapped["Film"] = relationship("Film", back_populates="film_actors")
```

#### FilmCategory (电影-分类关联表)
```python
class FilmCategory(Base):
    __tablename__ = "film_category"

    film_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("film.film_id"), primary_key=True
    )
    category_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("category.category_id"), primary_key=True
    )

    # 双向关系
    film: Mapped["Film"] = relationship("Film", back_populates="film_categories")
    category: Mapped["Category"] = relationship("Category", back_populates="film_categories")
```

### 6. 其他重要模型

#### Inventory (库存模型)
```python
class Inventory(Base, TimestampMixin):
    __tablename__ = "inventory"

    inventory_id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    film_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("film.film_id"), nullable=False
    )
    store_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("store.store_id"), nullable=False
    )

    # 关系：电影、商店、租赁
    film: Mapped["Film"] = relationship("Film", back_populates="inventories")
    store: Mapped["Store"] = relationship("Store", back_populates="inventories")
    rentals: Mapped[List["Rental"]] = relationship(
        "Rental", back_populates="inventory", cascade="all, delete-orphan"
    )
```

#### Category (分类模型)
```python
class Category(Base, TimestampMixin):
    __tablename__ = "category"

    category_id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    name: Mapped[str] = mapped_column(String(25), nullable=False)

    # 关系：电影分类关联
    film_categories: Mapped[List["FilmCategory"]] = relationship(
        "FilmCategory", back_populates="category", cascade="all, delete-orphan"
    )
```

## 数据关系设计

### 1. 一对多关系 (1:N)
- **Store → Customer**: 一个商店有多个客户
- **Film → Inventory**: 一部电影有多个库存副本
- **Customer → Rental**: 一个客户有多个租赁记录
- **Customer → Payment**: 一个客户有多个支付记录

### 2. 多对多关系 (M:N)
- **Film ↔ Actor**: 通过FilmActor关联表
- **Film ↔ Category**: 通过FilmCategory关联表

### 3. 一对一关系 (1:1)
- **Rental → Payment**: 一个租赁对应一个支付记录

### 4. 外键约束设计
- 所有外键都定义适当的约束
- 级联删除配置确保数据一致性
- 索引优化查询性能

## 数据规范化设计

### 1. 第一范式 (1NF)
- 每个字段都是原子的
- 没有重复的列
- 每行有唯一标识符

### 2. 第二范式 (2NF)
- 满足1NF
- 所有非主键字段完全依赖于主键
- 消除部分依赖

### 3. 第三范式 (3NF)
- 满足2NF
- 消除传递依赖
- 非主键字段之间没有依赖关系

## 索引设计策略

### 1. 主键索引
- 所有主键自动创建索引
- 支持快速查找和连接操作

### 2. 外键索引
- 外键列创建索引
- 优化关联查询性能

### 3. 查询优化索引
- `film.title`: 电影标题搜索
- `actor.last_name`: 演员姓氏搜索
- `customer.email`: 客户邮箱查询

### 4. 复合索引设计
```python
# Film表的索引定义
__table_args__ = (
    Index("idx_film_title", "title"),
    Index("idx_fk_language_id", "language_id"),
    Index("idx_fk_original_language_id", "original_language_id"),
)
```

## 数据约束设计

### 1. NOT NULL约束
- 业务必填字段设置NOT NULL
- 确保数据完整性

### 2. 唯一约束
- 电子邮件地址可能设置唯一约束
- 避免数据重复

### 3. 检查约束
- 评分等级约束（G, PG, PG-13等）
- 租期范围约束
- 租金金额范围

### 4. 外键约束
- 引用完整性保证
- 级联操作配置

## 时间戳设计

### 1. 自动时间戳
```python
class TimestampMixin:
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        server_default=func.now(),
        nullable=False
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        server_default=func.now(),
        onupdate=func.now(),
        nullable=False
    )
```

### 2. 业务时间字段
- `rental.rental_date`: 租赁日期
- `rental.return_date`: 归还日期
- `customer.create_date`: 客户创建日期
- `customer.last_update`: 最后更新日期

## 数据类型设计

### 1. 字符串类型
- `VARCHAR(45)`: 名字、姓氏等短文本
- `VARCHAR(255)`: 电影标题等较长文本
- `TEXT`: 电影描述等长文本
- `VARCHAR(100)`: 特殊功能等中等文本

### 2. 数值类型
- `INTEGER`: ID、年份、长度等整数
- `NUMERIC(4,2)`: 租金等小数（4位总数，2位小数）
- `NUMERIC(5,2)`: 赔偿成本等小数
- `BOOLEAN`: 布尔值状态

### 3. 时间类型
- `TIMESTAMP`: 日期时间（无时区）
- `TIMESTAMPTZ`: 带时区的日期时间
- `DATE`: 仅日期

## 模型继承与复用

### 1. Base类继承
```python
class Base(DeclarativeBase):
    """所有SQLAlchemy模型的基类"""
    pass
```

### 2. TimestampMixin混入
```python
class TimestampMixin:
    """为模型添加时间戳字段的混入类"""
```

### 3. 模型复用模式
- 基础字段通过混入类复用
- 关系定义在具体模型中
- 索引和约束在表参数中定义

## 性能考虑

### 1. 查询优化
- 适当的索引设计
- 延迟加载配置
- 批量查询优化

### 2. 数据分区
- 大型表可以考虑按时间分区
- 租赁表按年份分区
- 支付表按月份分区

### 3. 归档策略
- 历史数据归档设计
- 活跃数据与归档数据分离
- 定期数据清理策略

## 总结

Sakila数据库模型设计体现了企业级应用的数据建模最佳实践：
1. **规范化设计**确保数据一致性和完整性
2. **合理的关系设计**支持复杂的业务查询
3. **索引策略**优化查询性能
4. **约束设计**防止无效数据进入系统
5. **时间戳管理**提供完整的数据变更历史

这种设计既满足了DVD租赁业务的数据存储需求，又为高效的查询和分析提供了良好的基础。模型设计考虑了未来的扩展性，便于添加新的业务功能。